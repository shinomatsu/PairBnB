<%=  search_form_for(@q, url: search_path) do |f| %>
  <%= f.label :"which select is coutnry?"%>
  <%= f.collection_select :country, Listing.all, :country, :country %>
  <%= f.submit'Search'%>
<% end %>
